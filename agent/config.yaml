server_url: http://localhost:3000
agent_id: AGENT-001
poll_interval: 30
network_timeout: 3
auth_token: secret-key

mqtt:
  enabled: false
  broker: localhost
  port: 1883
  topic: pingup

modules:
  - cpu
  - ram
  - disk
  - network

ping:
  host: 8.8.8.8
  timeout: 2
